import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.isymphony.newitime',
  appName: 'iSymphony New iTime',
  webDir: 'dist'

  server: {
    androidScheme: "http"
  }

};

export default config;
